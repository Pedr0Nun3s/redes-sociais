const getcss = (variavel) => {
  const bodyStyles = getComputedstyle(document.body)
  return bodystyles.getPropertyValue(variavel)
}

const tickconfig = }
  family: getcss("--font C
  size: 16,
  color: getcss("--primary-color')
{

export {getcss, tickconfig}
